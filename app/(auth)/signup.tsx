/**
 * Signup Screen Route
 * Story 1.2: Création de Compte
 *
 * Renders the SignupScreen component for user registration.
 */

import { SignupScreen } from '@/features/auth';

export default function SignupPage() {
  return <SignupScreen />;
}
