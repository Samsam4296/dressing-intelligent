/**
 * Verify Email Screen Route
 * Story 1.2: Création de Compte
 *
 * Renders the VerifyEmailScreen component after successful signup.
 */

import { VerifyEmailScreen } from '@/features/auth';

export default function VerifyEmailPage() {
  return <VerifyEmailScreen />;
}
